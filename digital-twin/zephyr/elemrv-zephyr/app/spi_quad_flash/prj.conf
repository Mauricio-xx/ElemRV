# SPI Quad I/O flash test — reads MT25Q-style flash via nafarr SPI
# controller. Uses Zephyr SPI driver for RDID and direct register pokes
# for the Quad I/O Fast Read path (Zephyr's SPI API only supports single
# I/O; QIO needs the controller's mode-bits in the command stream).
CONFIG_SPI=y
