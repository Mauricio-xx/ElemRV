# Multi-node IoT gateway — receives DATA frames, sends ACKs, aggregates and reports
# Polling UART RX for data (no interrupt-driven UART)

CONFIG_PRINTK=y
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=n
